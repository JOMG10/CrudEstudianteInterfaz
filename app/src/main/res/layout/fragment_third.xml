<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".ThirdFragment">

    <!-- TODO: Update blank fragment layout -->
    <TextView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:gravity="center_horizontal"
        android:layout_marginTop="0dp"
        android:textSize="30sp"
        android:fontFamily="sans-serif-black"
        android:text="BUSCAR ESTUDIANTE"
        android:textColor="@color/green_normal"/>

    <EditText
        android:id="@+id/txtBuscarEstudiante"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Numero de control del estudiante"
        android:layout_marginTop="80dp"
        />
    <TextView
        android:id="@+id/txtEstudiantes"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Ningun resultado"
        android:textSize="18sp"
        android:layout_marginTop="200dp"/>

    <Button
        android:id="@+id/btnBuscarEstudiante"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="120dp"
        android:onClick="buscarEstudiante"
        android:text="buscar Estudiante"
        />


</FrameLayout>